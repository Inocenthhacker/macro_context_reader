---
PRD-001: Project Structure & Repository Setup
Status: Done
Owner: Hrimiuc
Layer: Infrastructure
Updated: 2026-04-15
Depends on: None
Blocks: All other PRDs (foundation)
---

## Obiectiv

Scafoldul minim reproducibil al proiectului: src-layout, pyproject.toml cu dev extras, pytest configurabil, .gitignore cu secrets/data ignorate, .env.example pentru onboarding. Fără acest PRD, niciun alt PRD nu poate fi implementat.

## Cerințe Funcționale

- [x] REQ-1: Python 3.11+ cerut în pyproject.toml (`requires-python = ">=3.11"`)
- [x] REQ-2: src-layout (`src/macro_context_reader/`) — package installable cu `pip install -e .`
- [x] REQ-3: `[project.optional-dependencies].dev` cu pytest, jupyter, macrosynergy, ewstools
- [x] REQ-4: `.env.example` cu variabile document (FRED_API_KEY etc.)
- [x] REQ-5: `.gitignore` protejează `.env`, `.env.local`, `secrets/`, `data/raw/*`, `data/processed/*`, modele binare (`*.pt`, `*.bin`)
- [x] REQ-6: Folder structure conform CLAUDE.md (notebooks/, src/, tests/, data/, config/, prds/)

## Cerințe Non-Funcționale

- [x] NFR-1: `pip install -e ".[dev]"` pornește curat într-un venv gol
- [x] NFR-2: `pytest` se poate rula din root fără PATH hack
- [x] NFR-3: Secretele nu ajung în git (verificat prin .gitignore)

## Abordare Tehnică

**Pattern:** src-layout (izolare între codul instalabil și tooling), `pyproject.toml` PEP 621, zero `setup.py`. Dependențele sunt adăugate incremental per PRD (D13) — pyproject conține doar ce e efectiv folosit de PRD-urile implementate.

**Stack:** `fredapi`, `cot-reports`, `pydantic`, `pyarrow`, `python-dotenv`, `ecbdata`, `hmmlearn`, `scikit-learn`, `scipy`, `transformers`, `torch`, `sentence-transformers`, `openai`, `spacy`, `requests`, `beautifulsoup4`. Dev extras: `pytest`, `jupyter`, `macrosynergy`, `ewstools`.

## Acceptance Criteria

- [x] AC-1: `git clone <repo> && cd macro_context_reader && python -m venv .venv && pip install -e ".[dev]"` succedă într-un shell curat
- [x] AC-2: `pytest -m "not integration"` trece cu ≥150 tests green
- [x] AC-3: `python -c "import macro_context_reader"` nu aruncă ImportError
- [x] AC-4: `.env.example` există și conține cheile minime pentru FRED
- [x] AC-5: Folder structure conformă cu CLAUDE.md (verificat manual)

## Claude Code Prompts

- [x] CC-1: Initial scaffold (pyproject.toml, .gitignore, src-layout) — Done (istoric)

## Note / Întrebări Deschise

- Extensii viitoare la dependencies se adaugă per PRD (D13 — incremental dependency policy)
- Pattern de testare integration/unit controlat prin pytest markers (`-m "not integration"`)
